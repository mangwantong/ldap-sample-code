# The classpath to use when generating an executable shell script for
# the launchable classes, for example,
# samplecode.EffectiveRightsEntryDemo
#
# For testing purposes only.
#
classpath = /Users/terrygardner/workspace/sample-code/ldap-sample-code/target/classes:/Users/terrygardner/UnboundID/software/ldapsdk/unboundid-ldapsdk-2.2.1-ce/unboundid-ldapsdk-ce.jar

# fully-qualified path name to 'dbtest'
#
# For testing purposes only.
#
dbtestPath = /Users/terrygardner/servers/ds/1389/bin/dbtest

# The name of the backend.
#
# For testing purposes only.
#
backendId = userRoot

# The name of a database by the DumpDatabasContainerTest test harness.
#
# For testing purposes only.
#
databaseName = cn.equality

# A base object: used in searches and for index testing.
#
# For testing purposes only.
#
baseDn = DC=example,DC=com

# JVM options to place in the generated script.
jvmOptions =-Dcom.unboundid.ldap.sdk.debug.level=WARNING -Dcom.unboundid.ldap.sdk.debug.includeStackTrace=false -Dcom.unboundid.ldap.sdk.debug.enabled=true -d64 -client -Xms32m -Xmx4096m -XX:+UseConcMarkSweepGC -XX:+CMSConcurrentMTEnabled -XX:+CMSParallelRemarkEnabled -XX:+CMSParallelSurvivorRemarkEnabled -XX:+CMSScavengeBeforeRemark -XX:RefDiscoveryPolicy=1 -XX:ParallelCMSThreads=1 -XX:CMSMaxAbortablePrecleanTime=3600000 -XX:CMSInitiatingOccupancyFraction=80 -XX:+UseParNewGC -XX:+UseMembar -XX:+UseBiasedLocking -XX:+UseCompressedOops -XX:+HeapDumpOnOutOfMemoryError

# The destination directory to use when generating an executable shell
# script for the launchable classes, for example,
# samplecode.EffectiveRightsEntryDemo
#
# For testing purposes only.
#
scriptDirectory = /Users/terrygardner/bin

# The class name to use when generating an executable shell script for
# the launchable classes, for example,
# samplecode.EffectiveRightsEntryDemo
#
# For testing purposes only.
#
launchableClassname = samplecode.EffectiveRightsEntryDemo


# An entry DN to use in testing. The value provided must evaluate
# to a valid distinguished name (not necessarily an existing DN, though).
#
# For testing purposes only.
#
entryDn = uid=user.0,ou=people,dc=example,dc=com

# the existing password of the entry DN
#
# For testing purposes only.
#
existingPassword = password

# a new password
#
# For testing purposes only.
#
newPassword = password

# The number of threads a multi-threaded application
# will use
numThreads = 3
